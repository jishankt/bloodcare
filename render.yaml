services:
  - type: web
    name: bloodcare
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bloodcare.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: bloodcare.settings
      - key: PYTHON_VERSION
        value: 3.11
